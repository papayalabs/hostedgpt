---
development:
  concurrency: 1
  timeout: 25
  verbose: true

test:
  concurrency: 1
  timeout: 25

production:
  concurrency: <%= ENV.fetch("SIDEKIQ_CONCURRENCY", 5) %>
  timeout: 25
  maxretry: 5
