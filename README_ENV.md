# Add direnv support for automatic environment variable loading
# Install direnv if not present: brew install direnv
# Then add the following line to your shell profile (e.g., ~/.zshrc or ~/.bashrc):
#
#   eval "$(direnv hook zsh)"  # or bash
#
# This will automatically load .envrc when you enter the repo directory.
